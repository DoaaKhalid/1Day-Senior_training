#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s,t;
  while(cin>>s>>t)
  {    int j=0;
            for(int i=0;i<t.size();i++)
                if(s[j]==t[i])
                   j++;
if(j==s.size())
    cout<<"Yes"<<endl;
else
    cout<<"No"<<endl;

  }
        return 0;
}
